apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: llm-serve
  labels: {app: llm-serve}
spec:
  selector:
    matchLabels: {app: llm-serve}
  endpoints:
    - port: http
      path: /metrics
      interval: 5s
