global:
  scrape_interval: 5s        # short on purpose: decode steps are ~20 ms, queues build in seconds
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: llm-serve
    metrics_path: /metrics
    static_configs:
      - targets: ["engine:8000"]
